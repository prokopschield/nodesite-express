import factory, { NodeSiteExpress } from "./nodesite-express";

export {
	NodeSiteExpress,
}

export default factory;
module.exports = factory;

Object.assign(factory, {
	default: factory,
	factory,
	NodeSiteExpress,
});
